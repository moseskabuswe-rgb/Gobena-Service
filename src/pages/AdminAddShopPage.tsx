// Replaced by AdminShopsPage modal — this file kept to prevent build errors
import { Navigate } from 'react-router-dom';
export default function AdminAddShopPage() {
  return <Navigate to="/admin/shops" replace />;
}
