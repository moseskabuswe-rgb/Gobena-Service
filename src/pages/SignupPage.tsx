// Replaced by AuthPages RegisterPage — this file kept to prevent build errors
import { Navigate } from 'react-router-dom';
export default function SignupPage() {
  return <Navigate to="/register" replace />;
}
